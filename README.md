# Bilkul new repo (DeepSeek + Flask)

## Setup
1. Add your DeepSeek API key in Render environment variables:
   DEEPSEEK_API_KEY=your_api_key_here

2. Deploy on Render by connecting this repo.

3. Test endpoint:
   POST /task
   {
     "command": "Instagram caption generate karo"
   }
